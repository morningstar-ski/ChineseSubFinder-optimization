services:
  chinesesubfinder:
    # Legacy compatibility override for users already calling
    # `docker compose -f compose.yaml -f compose.browser.yaml`.
    build:
      args:
        LITE_MODE: "false"
        INSTALL_BROWSER: "true"
    image: ${CSF_BROWSER_IMAGE:-weishaw/chinesesubfinder:local-browser}
