# SubtitleCat 接入回退链执行计划

## 1. 已确认的产品结论

本计划基于以下已确认边界执行，不再把这些点作为待定问题：

1. `SubtitleCat` 要接入现有字幕回退体系。
2. 在 **中文字幕下载链** 里，`SubtitleCat` 必须做成 **单独显式开关**，默认不与普通中文字幕源混在一起无感启用。
3. 在 **英文字幕回退链** 里，`SubtitleCat` 可以作为 **默认启用的兜底来源**。
4. `SubtitleCat` 不应再和 `LLM fallback` 绑定成同一语义层。
5. 结果进入最终选字幕阶段时，必须保留“翻译型结果”的可识别信息，避免污染原生中文字幕优先级。

## 2. 本轮目标

把 `SubtitleCat` 接入方案落成一份可直接执行的架构与开发计划，覆盖：

1. 接入位置。
2. 后端结构改造点。
3. 配置改造点。
4. 前端开关改造点。
5. 排序与质量保护规则。
6. 本地验证矩阵。

本文件是执行计划，不是评审提纲。

## 3. 总体接入策略

### 3.1 中文字幕链

`SubtitleCat` 作为 **翻译型中文字幕回退源** 存在，必须满足：

1. 单独配置。
2. 单独开关。
3. 默认关闭。
4. 不与 `xunlei / shooter / assrt / subhd` 这类原生中文字幕源混为同级默认主链。
5. 只有在需要时才参与中文字幕候选集。

### 3.2 英文字幕回退链

`SubtitleCat` 作为 **默认启用的英文回退链兜底来源** 存在，目标是：

1. 当原生中文未命中时，尽可能补到可用英文或可翻译底稿。
2. 不要求用户显式打开英文链使用它。
3. 但仍要受站点稳定性、失败重试和速率保护控制。

### 3.3 与 LLM fallback 的关系

`SubtitleCat` 必须从现有 `LLM fallback` 语义里拆出来，成为单独的一层。最终链路要能表达三种不同能力：

1. 英文字幕底稿 fallback。
2. 翻译型中文字幕 fallback。
3. LLM 字幕处理或生成 fallback。

这三层必须能独立开关、独立判定、独立测试。

## 4. 目标结构

建议将回退体系整理成下面的阶段模型：

1. **原生中文字幕主链**
   - 现有中文站点与中文源
2. **英文底稿回退链**
   - 现有英文 fallback supplier
   - `SubtitleCat` 默认启用兜底
3. **翻译型中文字幕回退链**
   - `SubtitleCat` 中文翻译结果
   - 受单独显式开关控制
4. **LLM fallback**
   - 保持单独阶段

这次改造的关键不是“多加一个 supplier”，而是把阶段语义拆清楚。

## 5. 执行步骤

### Step 0: 固化当前基线

目标：

把当前代码里现有链路、门控和配置现状固定下来，作为改造前基线。

执行项：

1. 记录 `buildSubSupplierHub()` 当前装配逻辑。
2. 记录普通 supplier、英文 fallback supplier 的当前列表。
3. 记录 `download_processer.go` 当前电影和剧集 fallback 触发条件。
4. 记录 `subtitle_sources.go` 当前配置结构。
5. 记录 `SettingsPanelSubSource.vue` 当前字幕源 UI。

输出物：

1. 当前链路基线说明。
2. 关键文件与关键行号列表。

完成标准：

可以明确指出现在的中文链、英文链、LLM 链分别在哪里触发。

### Step 1: 拆分回退阶段语义

目标：

从代码结构上把“英文回退”“翻译型中文字幕回退”“LLM fallback”分开。

执行项：

1. 审计 `SubSupplierHub` 是否只支持：
   - 普通 supplier
   - 英文 fallback supplier
2. 设计新的阶段表达方式，至少要能容纳：
   - 普通中文字幕 supplier
   - 英文 fallback supplier
   - 翻译型中文字幕 fallback supplier
3. 确定是：
   - 扩展 `SubSupplierHub`
   - 还是新增专门的 fallback stage 结构
4. 明确 `SubtitleCat` 在后端里分别挂载到哪两处：
   - 英文链默认兜底
   - 中文链显式翻译兜底

输出物：

1. 后端结构改造方案。
2. 新阶段模型说明。

完成标准：

代码结构上可以明确表达“同一个站点在英文链默认启用，但在中文翻译链显式开关控制”。

### Step 2: 重构触发门控

目标：

把现有与 `LLM fallback` 耦合的触发条件拆开。

执行项：

1. 审计电影流程里主链失败后的所有分支。
2. 审计剧集流程里主链失败后的所有分支。
3. 找出现在英文 fallback 被 `canTryLLMStageFallback()` 约束的全部位置。
4. 设计新的独立门控：
   - `canTryEnglishFallback()`
   - `canTryTranslatedChineseFallback()`
   - `canTryLLMStageFallback()`
5. 规定 `SubtitleCat` 的两种使用路径：
   - 英文链路径默认启用
   - 中文翻译链路径显式开关控制

输出物：

1. 新门控设计。
2. 旧耦合点清单。

完成标准：

关闭 LLM 时，`SubtitleCat` 仍可按既定产品策略参与英文链和中文字幕翻译链。

### Step 3: 设计 SubtitleCat 的双角色接入

目标：

让 `SubtitleCat` 在系统里以两个清晰角色存在，而不是一个模糊 supplier。

执行项：

1. 设计英文链角色：
   - 默认启用
   - 用于补英文底稿或可转化底稿
2. 设计中文字幕链角色：
   - 显式开关
   - 仅在用户允许翻译型中文字幕时参与
3. 确定是否需要：
   - 一个 supplier 实例带两种 capability
   - 两个 supplier 入口
   - 或一个 supplier + 两个装配入口
4. 明确失败行为：
   - 站点失败时不中断主流程
   - 不进入无限重试
   - 不因英文链失败错误标记中文链失败

输出物：

1. `SubtitleCat` 双角色设计。
2. 装配策略说明。

完成标准：

系统里能清晰区分 `SubtitleCat` 的英文兜底职责和中文字幕翻译职责。

### Step 4: 增加配置模型

目标：

把产品边界落实到配置结构。

执行项：

1. 在 `pkg/settings` 里新增 `SubtitleCat` 配置结构。
2. 至少包含：
   - `enabled`
   - `enable_translated_chinese_fallback`
   - 如有必要再加站点参数和保护项
3. 定义默认值：
   - `enabled = true` 或等价语义，用于英文链默认启用
   - `enable_translated_chinese_fallback = false`
4. 确定配置序列化字段与向后兼容行为。
5. 确定无配置升级时的默认行为。

输出物：

1. 配置模型草案。
2. 默认值与兼容策略。

完成标准：

通过配置就能实现：

1. 英文链默认有 `SubtitleCat`。
2. 中文翻译链默认没有 `SubtitleCat`。
3. 用户可以单独打开中文翻译型回退。

### Step 5: 前端设置面改造

目标：

把“英文链默认启用、中文链显式开关”明确暴露给用户。

执行项：

1. 在 `SettingsPanelSubSource.vue` 增加 `SubtitleCat` 设置块。
2. 明确区分两个含义：
   - `SubtitleCat` 英文回退链启用状态
   - 允许 `SubtitleCat` 参与中文字幕翻译回退
3. UI 上要避免让用户误解为：
   - 一开就会覆盖全部中文字幕下载逻辑
   - 一关就完全禁用英文兜底
4. 设计简洁但不含糊的文案。
5. 必要时放到高级设置区。

输出物：

1. 前端配置交互方案。
2. 文案草案。

完成标准：

用户能直观看懂：

1. 英文链默认已经在用它。
2. 中文字幕翻译回退需要自己显式打开。

### Step 6: 增加结果元数据与排序保护

目标：

防止翻译型中文字幕挤掉原生中文字幕。

执行项：

1. 给 `SubtitleCat` 产出的结果增加可识别信息，至少应能表达：
   - 是否为翻译型结果
   - 原始语言
   - 来源 supplier
2. 审计当前 `SubInfo` 和后续选择流程，决定 metadata 放置点。
3. 调整排序规则：
   - 原生中文优先
   - 原生双语中文次之
   - 翻译型中文字幕后置
4. 确保只有在“没有更好原生中文”时，翻译型中文字幕才有机会落地。
5. 确保最终命名和来源标识可追踪。

输出物：

1. 元数据方案。
2. 排序规则改造方案。

完成标准：

即使打开了中文字幕翻译回退，原生中文仍然稳定优先。

### Step 7: 设计失败保护与维护边界

目标：

避免接入后把链路变脆。

执行项：

1. 定义 `SubtitleCat` 请求失败的处理方式。
2. 定义页面变更、翻译失败、下载失败、超时等场景的退出策略。
3. 限制最大尝试次数。
4. 防止它引发重复抓取、空转、错误级联。
5. 设计日志标记，便于后续回归判断：
   - 英文链触发
   - 中文翻译链触发
   - 结果被接受或被降权淘汰

输出物：

1. 失败保护策略。
2. 日志与观测点设计。

完成标准：

`SubtitleCat` 失败时不会把整条任务链拖坏。

### Step 8: 制定实现任务清单

目标：

把架构决策转成可以直接开工的任务列表。

执行项：

1. 列出后端改动文件。
2. 列出前端改动文件。
3. 列出配置改动文件。
4. 列出测试改动文件。
5. 列出需要新增的 supplier 文件或辅助文件。

建议任务分组：

1. 阶段模型与 hub 改造
2. 下载流程门控拆分
3. SubtitleCat supplier 接入
4. 配置与前端
5. 排序与 metadata
6. 单测与集成测试
7. 本地真实回归

输出物：

1. 开发任务清单。
2. 文件级变更清单。

完成标准：

下一轮可以直接按任务清单编码，不再重新讨论边界。

## 6. 验证矩阵

### 6.1 单元测试

至少覆盖：

1. `SubtitleCat` 被装配进英文 fallback 链。
2. `SubtitleCat` 不会默认进入中文字幕翻译链，除非显式开关开启。
3. `LLM fallback` 关闭时，英文链 `SubtitleCat` 仍可运行。
4. `LLM fallback` 关闭时，中文字幕翻译链也只受自己的显式开关控制。
5. 翻译型中文字幕 metadata 正确透传。
6. 排序时原生中文优先于翻译型中文字幕。

### 6.2 集成测试

至少覆盖：

1. 主链已有可用中文时，不落到 `SubtitleCat` 中文翻译链。
2. 主链无中文时，英文 fallback 能使用 `SubtitleCat`。
3. 开启中文字幕翻译回退时，可从 `SubtitleCat` 产出中文字幕候选。
4. 关闭中文字幕翻译回退时，即使英文链用了 `SubtitleCat`，也不自动把它当成中文字幕结果。
5. `SubtitleCat` 失败时，主流程正常结束，不出现死循环或错误升级。

### 6.3 本地真实回归

建议覆盖以下样本：

1. 无原生中文、可找到英文底稿的电影。
2. 无原生中文、可找到英文底稿的剧集单集。
3. 有原生中文的样本，确认翻译型中文字幕不会抢占。
4. `SubtitleCat` 不可用样本，确认任务不会卡死。
5. 打开中文字幕翻译开关的样本，确认确实能补出中文字幕结果。

## 7. 风险重点

### high

1. 仍然把 `SubtitleCat` 跟 `LLM fallback` 共用一个门控。
2. 翻译型中文字幕没有降权，直接污染原生中文选择。
3. 英文链和中文链共用一个开关，导致产品语义混乱。

### medium

1. 同一个 supplier 的双角色实现不清晰，后续维护困难。
2. 前端文案含糊，用户无法理解为什么英文链默认生效但中文链要单独打开。
3. 结果日志无法看出是英文链命中还是中文字幕翻译链命中。

### low

1. 设置面摆放位置不理想。
2. 命名不统一。

## 8. 最终完成标准

本计划执行完成时，必须满足：

1. `SubtitleCat` 在英文回退链中默认启用的接入方案已明确。
2. `SubtitleCat` 在中文字幕下载链中显式开关的接入方案已明确。
3. 与 `LLM fallback` 的耦合拆分方案已明确。
4. 排序与 metadata 保护方案已明确。
5. 本地验证矩阵已明确。
6. 后续实现可以直接按步骤推进。
